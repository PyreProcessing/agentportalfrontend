export default interface UserType {
  _id: string;
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  profileImageUrl: string;
  profile: any;
  createdAt: string;
  updatedAt: string;
}
